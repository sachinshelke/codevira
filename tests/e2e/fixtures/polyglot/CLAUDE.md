# Polyglot Fixture — Claude Code Instructions

Top-level CLAUDE.md. Codevira's `init` should include `.` in
watched_dirs because this top-level markdown file is part of the
codebase. Bug F: `detect_watched_dirs` skips top-level files.
