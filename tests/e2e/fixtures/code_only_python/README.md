# Code-Only Python Fixture

A small but realistic Python project — `pyproject.toml`, a `src/`
package with a few modules, and unit tests. Represents the canonical
"happy path" for codevira's indexer.

If this fixture FAILS the gauntlet, codevira is broken in its core
use case. This is the smoke test.
