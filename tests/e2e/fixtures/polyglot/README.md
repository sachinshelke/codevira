# Polyglot Fixture

A project with Python + TypeScript + YAML + Markdown side by side —
the realistic shape of modern web apps and SaaS codebases. Represents
the bug-F surface where init drops top-level files because
`detect_watched_dirs` filters by single-language extensions.
