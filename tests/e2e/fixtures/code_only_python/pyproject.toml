[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "code-only-python-fixture"
version = "0.1.0"
description = "Sample Python project for codevira's first-contact gauntlet."
requires-python = ">=3.10"
